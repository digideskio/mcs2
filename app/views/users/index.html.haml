%div#userlist
  %div#userfilters
  
  %ul#userlist-ul
    - @users.each do |user|
      %li{:class=>"user", :id => user.id} 
        %a{:href => user_path(user.id) }
          %div.userimg
            fgfd
          %div.username
            = user.username
          

  %br.clearboth
