%div#serverlist
  -if user_signed_in?
    = link_to 'New Server', new_server_path

  %ul#serverlist-ul
    - @servers.each do |server|
      %li.server
        %h1
          =link_to server.name, server


